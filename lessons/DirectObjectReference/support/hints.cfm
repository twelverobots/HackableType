<cf_layout>
	<cfscript>
		aHints = [
			"This exercise requires parameter manipulation",
			"You will need to use a browser plugin or a proxy to change the value of a form field",
			"The value that needs to be change directly references a record in the database",
			"By looking at the existing records, we can see that the developer ennumerated the values, so guessing other values is easy"
		];
	</cfscript>
	
	<cf_hintLayout ahints="#aHints#" />
</cf_layout>